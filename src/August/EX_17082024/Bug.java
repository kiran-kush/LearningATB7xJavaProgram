package August.EX_17082024;

public enum Bug {
       LOW,
       Medium,
       High
    // enum is used to define constant
       // enum is used to stored some static constant value from the code
       //so the constant could be accessible by everyone
       // it becoms a public utility and can be accessed outside the package as well

}
